---
const testimonials = [
  {
    name: "Carlos Mendoza",
    role: "CEO, TechStart México",
    company: "TechStart México",
    image: "/testimonials/carlos.webp",
    testimonial: "The Holy Code transformó nuestra idea de startup en una plataforma real en tiempo récord. Su enfoque indie nos permitió mantener la agilidad mientras construíamos algo sólido y escalable.",
    project: "MVP de plataforma e-commerce",
    rating: 5,
    featured: true
  },
  {
    name: "María Elena Rodríguez",
    role: "Administradora",
    company: "Condominio Bosques del Valle",
    image: "/testimonials/maria.webp",
    testimonial: "La plataforma Kondominios revolucionó completamente nuestra gestión. Lo que antes tomaba días, ahora lo resolvemos en minutos. Mauro entendió exactamente nuestras necesidades.",
    project: "Sistema de gestión Kondominios",
    rating: 5,
    featured: false
  },
  {
    name: "Roberto Kim",
    role: "Fundador & CTO",
    company: "HealthTech Solutions",
    image: "/testimonials/roberto.webp",
    testimonial: "La consultoría técnica de The Holy Code fue crucial para escalar nuestro producto. Su experiencia en arquitectura nos ahorró meses de desarrollo y refactoring.",
    project: "Consultoría técnica y arquitectura",
    rating: 5,
    featured: false
  },
  {
    name: "Ana Sofía Morales",
    role: "Product Manager",
    company: "Innovación Digital",
    image: "/testimonials/ana.webp",
    testimonial: "Trabajar con The Holy Code fue como tener un cofundador técnico. Su compromiso con la calidad y la innovación superó todas nuestras expectativas.",
    project: "Desarrollo de aplicación móvil",
    rating: 5,
    featured: false
  }
];

const stats = [
  {
    number: "25+",
    label: "Proyectos completados",
    description: "Desde MVPs hasta productos escalables"
  },
  {
    number: "98%",
    label: "Satisfacción del cliente",
    description: "Basado en feedback y recomendaciones"
  },
  {
    number: "4.9/5",
    label: "Rating promedio",
    description: "Evaluación en proyectos completados"
  },
  {
    number: "2-8",
    label: "Semanas promedio",
    description: "De idea a producto funcional"
  }
];

const featuredTestimonial = testimonials.find(t => t.featured);
const regularTestimonials = testimonials.filter(t => !t.featured);
---

<div class="max-w-6xl mx-auto space-y-16">
  <!-- Header -->
  <div class="text-center">
    <h2 class="font-heading text-3xl md:text-4xl font-bold text-holy-text-primary mb-6">
      Lo que dicen nuestros clientes
    </h2>
          <p class="text-holy-text-secondary text-lg leading-relaxed max-w-3xl mx-auto">
      Cada proyecto es una oportunidad de crear algo extraordinario. 
      Estas son las historias de quienes confiaron en nosotros para transformar sus ideas en realidad.
    </p>
  </div>

  <!-- Estadísticas -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
    {stats.map((stat) => (
      <div class="text-center testimonial-stat-card backdrop-blur-sm rounded-xl p-6 border testimonial-border">
        <div class="text-2xl md:text-3xl font-bold text-holy-action mb-2">
          {stat.number}
        </div>
        <div class="font-heading font-semibold text-holy-text-primary mb-1">
          {stat.label}
        </div>
        <div class="text-sm text-holy-text-secondary">
          {stat.description}
        </div>
      </div>
    ))}
  </div>

  <!-- Testimonial destacado -->
  {featuredTestimonial && (
    <div class="testimonial-featured-container backdrop-blur-sm rounded-2xl p-8 md:p-12 border border-holy-action/20 relative overflow-hidden">
      <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-holy-action to-holy-action/50"></div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-center">
        <div class="md:col-span-2 space-y-6">
          <!-- Estrellas -->
          <div class="flex gap-1">
            {Array.from({ length: featuredTestimonial.rating }).map((_, i) => (
              <span class="text-holy-action text-xl">★</span>
            ))}
          </div>
          
          <!-- Testimonial -->
          <blockquote class="text-lg md:text-xl text-holy-text-secondary leading-relaxed italic">
            "{featuredTestimonial.testimonial}"
          </blockquote>
          
          <!-- Proyecto -->
          <div class="testimonial-project-card rounded-lg p-4 border testimonial-border">
            <p class="text-holy-text-secondary text-sm">Proyecto:</p>
            <p class="text-holy-text-primary font-semibold">{featuredTestimonial.project}</p>
          </div>
        </div>
        
        <!-- Autor con Avatar Espectacular -->
        <div class="text-center md:text-left">
          <div class="mb-6 flex justify-center md:justify-start">
            <div class="relative group">
              <!-- Avatar principal con efectos espectaculares -->
              <div class="relative">
                <img 
                  src={featuredTestimonial.image} 
                  alt={featuredTestimonial.name}
                  class="w-32 h-32 md:w-36 md:h-36 rounded-full object-cover transition-all duration-500 group-hover:scale-105"
                  style="image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges;"
                />
                <!-- Borde con gradiente espectacular -->
                <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-holy-action via-purple-500 to-blue-500 p-1">
                  <div class="w-full h-full rounded-full bg-holy-background"></div>
                </div>
                <!-- Imagen sobre el borde -->
                <img 
                  src={featuredTestimonial.image} 
                  alt={featuredTestimonial.name}
                  class="absolute inset-1 w-[calc(100%-8px)] h-[calc(100%-8px)] rounded-full object-cover transition-all duration-500 group-hover:scale-105 shadow-2xl"
                  style="image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges;"
                />
              </div>
              
              <!-- Sombra brillante animada -->
              <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-holy-action/30 via-purple-500/30 to-blue-500/30 blur-xl group-hover:blur-2xl transition-all duration-500 -z-10 scale-110 group-hover:scale-125"></div>
              
              <!-- Efecto de resplandor -->
              <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-holy-action/10 via-transparent to-blue-500/10 group-hover:from-holy-action/20 group-hover:to-blue-500/20 transition-all duration-500"></div>
            </div>
          </div>
          <div class="space-y-1">
            <p class="font-heading text-lg font-semibold text-holy-text-primary">
              {featuredTestimonial.name}
            </p>
            <p class="text-holy-action font-medium">
              {featuredTestimonial.role}
            </p>
            <p class="text-holy-text-secondary text-sm">
              {featuredTestimonial.company}
            </p>
          </div>
        </div>
      </div>
    </div>
  )}

  <!-- Testimonials grid -->
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {regularTestimonials.map((testimonial) => (
      <div class="testimonial-card backdrop-blur-sm rounded-xl p-6 border testimonial-border hover:border-holy-action/30 transition-all duration-300 group">
        <!-- Header con Avatar Mejorado -->
        <div class="flex items-center gap-4 mb-4">
          <div class="relative group/avatar">
            <!-- Avatar con efectos profesionales -->
            <div class="relative">
              <img 
                src={testimonial.image} 
                alt={testimonial.name}
                class="w-16 h-16 rounded-full object-cover transition-all duration-300 group-hover/avatar:scale-110"
                style="image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges;"
              />
              <!-- Borde con gradiente sutil -->
              <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-holy-action/50 via-purple-400/30 to-blue-400/50 p-0.5">
                <div class="w-full h-full rounded-full bg-holy-background"></div>
              </div>
              <!-- Imagen sobre el borde -->
              <img 
                src={testimonial.image} 
                alt={testimonial.name}
                class="absolute inset-0.5 w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full object-cover transition-all duration-300 group-hover/avatar:scale-110 shadow-lg"
                style="image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges;"
              />
            </div>
            
            <!-- Sombra brillante sutil -->
            <div class="absolute inset-0 rounded-full bg-gradient-to-tr from-holy-action/20 via-purple-400/10 to-blue-400/20 blur-lg group-hover/avatar:blur-xl transition-all duration-300 -z-10 scale-110 group-hover/avatar:scale-125"></div>
          </div>
          <div class="flex-1">
            <p class="font-heading font-semibold text-holy-text-primary">
              {testimonial.name}
            </p>
            <p class="text-holy-action text-sm">
              {testimonial.role}
            </p>
          </div>
        </div>
        
        <!-- Estrellas -->
        <div class="flex gap-1 mb-4">
          {Array.from({ length: testimonial.rating }).map((_, i) => (
            <span class="text-holy-action">★</span>
          ))}
        </div>
        
        <!-- Testimonial -->
        <blockquote class="text-holy-text-secondary text-sm leading-relaxed italic mb-4">
          "{testimonial.testimonial}"
        </blockquote>
        
        <!-- Proyecto -->
        <div class="pt-4 border-t testimonial-divider">
          <p class="text-holy-text-secondary text-xs">Proyecto:</p>
          <p class="text-holy-text-primary text-sm font-medium">{testimonial.project}</p>
        </div>
      </div>
    ))}
  </div>

  <!-- Call to action -->
  <div class="text-center testimonial-cta-container backdrop-blur-sm rounded-2xl p-8 border border-holy-action/20">
    <h3 class="font-heading text-2xl font-semibold text-holy-text-primary mb-4">
      ¿Listo para ser el próximo caso de éxito?
    </h3>
    <p class="text-holy-text-secondary mb-6 max-w-2xl mx-auto">
      Cada gran producto comienza con una conversación. Cuéntanos tu visión 
      y descubre cómo podemos transformarla en una solución digital excepcional.
    </p>
    
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <a 
        href="#contacto" 
        class="inline-flex items-center justify-center px-6 py-3 bg-holy-action text-white font-medium rounded-lg hover:bg-holy-action/90 transition-colors"
      >
        Agenda una consulta
      </a>
      <a 
        href="#servicios" 
        class="inline-flex items-center justify-center px-6 py-3 border border-holy-action/30 text-holy-action font-medium rounded-lg hover:bg-holy-action/10 transition-colors"
      >
        Conocer servicios
      </a>
    </div>
  </div>
</div>

<style>
  /* Estilos base para testimoniales */
  .testimonial-stat-card {
    background: var(--holy-background-secondary);
    border-color: var(--holy-border);
    transition: all 0.3s ease;
  }

  .testimonial-stat-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(var(--holy-action-rgb), 0.1);
  }

  .testimonial-featured-container {
    background: var(--holy-background-secondary);
    box-shadow: 0 20px 40px rgba(var(--holy-action-rgb), 0.1);
  }

  .testimonial-project-card {
    background: var(--holy-background);
    border-color: var(--holy-border);
  }

  .testimonial-card {
    background: var(--holy-background-secondary);
    border-color: var(--holy-border);
    transition: all 0.3s ease;
  }

  .testimonial-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 15px 35px rgba(var(--holy-action-rgb), 0.1);
  }

  .testimonial-border {
    border-color: var(--holy-border);
  }

  .testimonial-divider {
    border-color: var(--holy-border);
  }

  .testimonial-cta-container {
    background: var(--holy-background-secondary);
  }

  /* Mejoras para modo claro */
  :global(html.light) .testimonial-stat-card {
    background: rgba(255, 255, 255, 0.7);
    border-color: rgba(139, 92, 246, 0.1);
    backdrop-filter: blur(10px);
  }

  :global(html.light) .testimonial-featured-container {
    background: rgba(255, 255, 255, 0.8);
    border-color: rgba(139, 92, 246, 0.2);
    backdrop-filter: blur(20px);
  }

  :global(html.light) .testimonial-project-card {
    background: rgba(255, 255, 255, 0.9);
    border-color: rgba(139, 92, 246, 0.15);
    backdrop-filter: blur(10px);
  }

  :global(html.light) .testimonial-card {
    background: rgba(255, 255, 255, 0.8);
    border-color: rgba(139, 92, 246, 0.1);
    backdrop-filter: blur(15px);
  }

  :global(html.light) .testimonial-cta-container {
    background: rgba(255, 255, 255, 0.8);
    border-color: rgba(139, 92, 246, 0.2);
    backdrop-filter: blur(20px);
  }

  /* Efectos de sparkle para modo claro */
  :global(html.light) .testimonial-stat-card::before,
  :global(html.light) .testimonial-card::before,
  :global(html.light) .testimonial-featured-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 30%, rgba(139, 92, 246, 0.03) 50%, transparent 70%);
    border-radius: inherit;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  :global(html.light) .testimonial-stat-card:hover::before,
  :global(html.light) .testimonial-card:hover::before,
  :global(html.light) .testimonial-featured-container:hover::before {
    opacity: 1;
  }

  /* Animación de resplandor para avatares */
  @keyframes avatar-glow {
    0%, 100% { 
      box-shadow: 0 0 20px rgba(var(--holy-action-rgb), 0.3); 
    }
    50% { 
      box-shadow: 0 0 30px rgba(var(--holy-action-rgb), 0.5), 0 0 40px rgba(139, 92, 246, 0.3); 
    }
  }

  .group:hover .avatar-glow {
    animation: avatar-glow 2s ease-in-out infinite;
  }

  /* Optimización de imágenes */
  img {
    image-rendering: -webkit-optimize-contrast;
    image-rendering: optimize-contrast;
    image-rendering: crisp-edges;
  }
</style> 